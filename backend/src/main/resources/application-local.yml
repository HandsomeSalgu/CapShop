spring:
  docker:
    compose:
      enabled: false

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3307/syncshopper?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: root
    password: ${SPRING_DATASOURCE_PASSWORD}

  jackson:
    time-zone: Asia/Seoul

  data:
    redis:
      host: localhost
      port: 6379

server:
  servlet:
    encoding:
      charset: UTF-8
      enabled: true
      force: true
